# You are given an array of integers arr of size n. You are also given an array of queries q of size m. For each query, determine how many times the queried number appears in the original array.

from typing import List

def countFrequencies(arr: List[int], queries: List[int]) -> List[int]:
        